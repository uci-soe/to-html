'use strict';

var assert = require('assert');
//var loadTemplates = require('../lib/load-templates');

describe('Load Templates', function () {
  it('doesn\'t need testing att his time', function () {
    assert(true);
  });
});
